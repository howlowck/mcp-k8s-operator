apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  name: mcpservers.mcp.lifeishao.com
spec:
  group: mcp.lifeishao.com
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    singular: mcpserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPServer is the Schema for the mcpservers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer.
            properties:
              image:
                description: Docker Image of the MCP server
                type: string
              args:
                description: List of arguments for the command
                items:
                  type: string
                type: array
              command:
                description: Command to run, supports "docker", "npx", "uvx"
                type: string
              name:
                description: Name of the MCP server
                type: string
              transport:
                description: Transport type, either "stdio" or "streamable"
                type: string
            required:
            - args
            - command
            - name
            - transport
            type: object
            allOf:
            - if:
                properties:
                  command:
                    const: "docker"
              then:
                required:
                - image
          status:
            description: MCPServerStatus defines the observed state of MCPServer.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
